# The contact form posts directly to a Formspree endpoint hardcoded in
# pages/contact.tsx (form ID: mldlglyq) — no environment variables required.

# Optional — used by src/components/SEO.tsx for canonical/OG URLs.
NEXT_PUBLIC_SITE_URL=https://your-domain.vercel.app
